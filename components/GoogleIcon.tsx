import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

export default function GoogleIcon() {
  return <Icon name="google" size={20} color="#fff" style={{marginRight: 8}} />;
}
